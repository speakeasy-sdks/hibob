/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as z from "zod";

export type NewListItem = {
    /**
     * Name of the item.
     */
    name: string;
    /**
     * ID of the new hierarchy parent node.
     */
    parentId?: number | undefined;
};

/** @internal */
export namespace NewListItem$ {
    export type Inbound = {
        name: string;
        parentId?: number | undefined;
    };

    export const inboundSchema: z.ZodType<NewListItem, z.ZodTypeDef, Inbound> = z
        .object({
            name: z.string(),
            parentId: z.number().int().optional(),
        })
        .transform((v) => {
            return {
                name: v.name,
                ...(v.parentId === undefined ? null : { parentId: v.parentId }),
            };
        });

    export type Outbound = {
        name: string;
        parentId?: number | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, NewListItem> = z
        .object({
            name: z.string(),
            parentId: z.number().int().optional(),
        })
        .transform((v) => {
            return {
                name: v.name,
                ...(v.parentId === undefined ? null : { parentId: v.parentId }),
            };
        });
}
